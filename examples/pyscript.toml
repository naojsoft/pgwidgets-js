# PyScript configuration.
#
# Pull pgwidgets-js from PyPI via micropip instead of mapping the
# repo's source files in.  Bumps the install once — afterwards
# the wheel is cached by the browser.
packages = ["pgwidgets-js>=0.2.0"]
