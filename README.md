Pretty Good Widgets
===================
A set of minimalistic HTML5 widgets.

Running the Examples
====================
To run the examples, start a web server serving the top level of the
folder.  i.e.:

$ python -m http.server --bind localhost 8000

then navigate to one of the labelled HTML examples, i.e.

http://localhost:8000/examples/mdi_widget.html
